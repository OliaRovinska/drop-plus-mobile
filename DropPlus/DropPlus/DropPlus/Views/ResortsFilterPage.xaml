<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:DropPlus.Converters"
             x:Class="DropPlus.Views.ResortsFilterPage"
             Title="Фільтр">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IntEnumConverter x:Key="IntEnumConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="20, 10">

                <StackLayout Orientation="Horizontal">
                    <Label Text="Локація" TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" VerticalOptions="CenterAndExpand" />
                    <Entry Text="{Binding Location}" TextColor="{StaticResource TextColor}" WidthRequest="100" FontSize="Small" HorizontalOptions="EndAndExpand" VerticalOptions="Start" />
                </StackLayout>

                <Label Text="Хвороби:" TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" />
                <ListView ItemsSource="{Binding Diseases}" RowHeight="30" ItemSelected="ListView_OnItemSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                    <Switch IsToggled="{Binding IsChecked}" VerticalOptions="CenterAndExpand" />
                                    <Label Text="{Binding Name}" Margin="5, 0, 0, 0" FontSize="Small" TextColor="{StaticResource TextColor}" VerticalOptions="Center" />
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackLayout Orientation="Horizontal">
                    <Label Text="Рейтинг: " TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" VerticalOptions="CenterAndExpand" />
                    <Label Text="від" TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" VerticalOptions="CenterAndExpand" Margin="10, 0, 0, 0" />
                    <Entry Text="{Binding MinRating}" Keyboard="Numeric" TextColor="{StaticResource TextColor}" WidthRequest="30" FontSize="Small" VerticalOptions="Start" />
                    <Label Text="до" TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" VerticalOptions="CenterAndExpand" Margin="15, 0, 0, 0" />
                    <Entry Text="{Binding MaxRating}" Keyboard="Numeric" TextColor="{StaticResource TextColor}" WidthRequest="30" FontSize="Small" VerticalOptions="Start" />
                </StackLayout>

                <StackLayout Orientation="Horizontal">
                    <Label Text="Сезон: " TextColor="{StaticResource TextColor}" FontSize="Small" HorizontalOptions="Start" VerticalOptions="CenterAndExpand" />
                    <Picker ItemsSource="{Binding Seasons}" SelectedIndex="{Binding Season, Converter={StaticResource IntEnumConverter}}" TextColor="{StaticResource TextColor}" WidthRequest="100" FontSize="Small" HorizontalOptions="EndAndExpand" VerticalOptions="Start" />
                </StackLayout>

                <StackLayout Orientation="Horizontal">
                    <Switch IsToggled="{Binding IsSanatorium}" VerticalOptions="CenterAndExpand" />
                    <Label Text="Тільки санаторії" Margin="5, 0, 0, 0" FontSize="Small" TextColor="{StaticResource TextColor}" VerticalOptions="Center" />
                </StackLayout>

                <Button Margin="10, 0" Text="Застосувати" FontSize="Small" TextColor="{StaticResource TextColor}" VerticalOptions="EndAndExpand" HorizontalOptions="FillAndExpand" HeightRequest="40" Clicked="OnApplyFilterClick"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>